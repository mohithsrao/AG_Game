# Agent Rules

- **Branch Protection Constraint**: Do not commit or merge directly to the `main` or `master` branch. All development must occur on separate feature branches. Before merging, a Pull Request must be created and reviewed, and explicit user confirmation must be obtained.
